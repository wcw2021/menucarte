{% extends 'base.html.twig' %}

{% block title %}Edit Liste{% endblock %}

{% block body %}
<br>
<div class="container">
    <h1>Edit Liste</h1>
    <br>

    {{ include('liste/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-secondary my-2" href="{{ path('liste_index') }}">back to list</a>

    {{ include('liste/_delete_form.html.twig') }}
</div>
{% endblock %}
